#! /usr/bin/env bash
BUILD_DIR=$1
../python-bin/detect $BUILD_DIR $null > /dev/null
$pr = $?
../ruby-bin/detect $BUILD_DIR $null > /dev/null
$rr = $?
if [ $pr -eq 0 ]; then
    ../python-bin/release $BUILD_DIR
fi
if [ $rr -eq 0 ]; then
    ../ruby-bin/release $BUILD_DIR
fi
